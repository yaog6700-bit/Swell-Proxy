<Page
    x:Class="AnywhereWinUI.Views.RoutingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16" Spacing="4">
            <TextBlock Text="分流规则" FontSize="20" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="配置智能分流拦截规则，支持应用规则、自定义规则与顺序匹配" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="Transparent"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="24"
                MaxWidth="750"
                HorizontalAlignment="Left">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8" Margin="0,0,0,20">
                    <StackPanel Spacing="2" VerticalAlignment="Center">
                        <TextBlock Text="策略组配置" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                        <TextBlock x:Name="RuleCountText" Text="共 0 条规则，按列表顺序匹配" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <Button Grid.Column="1"
                            Click="ResetRulesButton_Click"
                            Padding="10,6"
                            CornerRadius="6"
                            ToolTipService.ToolTip="恢复默认规则和默认顺序">
                        <FontIcon Glyph="&#xE777;" FontSize="13"/>
                    </Button>

                    <Button Grid.Column="2"
                            Content="配置自定义规则"
                            Style="{ThemeResource AccentButtonStyle}"
                            FontSize="12"
                            Padding="14,6"
                            CornerRadius="6"
                            Click="AddRuleButton_Click"/>
                </Grid>

                <ListView x:Name="RulesListView"
                          Grid.Row="1"
                          SelectionMode="None"
                          CanDragItems="True"
                          CanReorderItems="True"
                          AllowDrop="True"
                          DragItemsCompleted="RulesListView_DragItemsCompleted"
                          ContainerContentChanging="RulesListView_ContainerContentChanging">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,8"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
